# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vite dependency cache
.vite

# Claude Code local settings
.claude/settings.local.json

*storybook.log
storybook-static

# Storybook built into public/ by build-storybook:embed so the app can serve it
# at /storybook. Build output, not source — regenerate it, do not commit it.
public/storybook
